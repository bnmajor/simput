from parflow import Run
from parflow.tools.fs import mkdir, get_absolute_path

run = Run('run', __file__)

{{{'#---------------------------------------------------------'}}}

test_run.pfset(flat_map={
  {{#each parameters}}
  {{{parameter}}}
  {{/each}}
})

dir_name = get_absolute_path('test_output/run')
mkdir(dir_name)
run.run(working_directory=dir_name)